export default function LobbyPage() {
  return (
    <main className="flex min-h-screen items-center justify-center bg-[color:var(--bg)] text-[color:var(--text-mute)]">
      <p className="font-ui-label text-sm">Lobby — stats &amp; leaderboard (placeholder)</p>
    </main>
  )
}
